#Write a program that lets the user guess whether a flipped coin displays the head or the tail. 
#The program randomly generates an integer 0 or 1, which represents head or tail. 
#The program prompts the user to enter a guess and reports whether the guess is correct or incorrect.

import random

coin = random.randint(0, 1)

print("Guess head or tail?")

guess = int(input("Enter 0 for head and 1 for tail: "))

if guess == coin:
    print("Correct")
else:
    print("Incorrect")
